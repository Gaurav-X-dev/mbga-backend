# MBGA Admin Web

React + TypeScript Admin Web Panel scaffold.

```powershell
npm install
npm run dev
npm run typecheck
npm run lint
npm run test
npm run build
```

Only public browser configuration belongs in `VITE_` variables. Do not copy backend secrets into this project.
